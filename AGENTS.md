# AGENTS.md

Design Cursor 是一个 Manifest V3 Chrome / Edge 扩展。它会在真实网页中注入一个紧凑的页面选择器，用来选中 DOM 元素或文本、编辑布局和样式意图，并复制适合 AI 协作修改 UI 的结构化上下文。

## 项目结构

- 这个仓库没有构建流程。扩展以“加载已解压的扩展程序”的方式直接从当前目录运行。
- `manifest.json` 定义权限、图标、浏览器工具栏入口，以及 `Command+Shift+Y` / `Ctrl+Shift+Y` 快捷键。
- `background.js` 是唯一的 service worker。它负责注入 `content.css` 和内容脚本，然后向当前标签页发送开关消息。
- 内容脚本按依赖顺序拆分并注入：
  1. `content/00-bootstrap.js` - 共享状态、模式文案、图标注册。
  2. `content/10-ui.js` - 工具栏、选择清单、输入面板、调整面板 DOM。
  3. `content/20-core.js` - DOM 工具、渲染、覆盖层、历史记录。
  4. `content/30-selection-layout.js` - 选择、悬停、层级切换、提示词、排版移动。
  5. `content/40-adjust.js` - 样式调整、颜色、阴影、数字拖拽控制。
  6. `content/50-runtime.js` - 激活、全局事件、键盘操作、剪贴板。

不要随意调整这些文件的注入顺序。后面的脚本依赖前面脚本暴露的全局状态和工具函数。

## 本地运行与验证

1. 打开 `chrome://extensions/` 或 `edge://extensions/`。
2. 开启开发者模式。
3. 选择当前仓库目录并加载为已解压扩展。
4. 修改代码后，先在扩展管理页重新加载扩展，再刷新目标网页。
5. 点击扩展图标，或使用快捷键启动：

```text
Mac: Command+Shift+Y
Windows: Ctrl+Shift+Y
```

只在普通 `http` 或 `https` 页面上验证。浏览器内部页面，例如 `chrome://` 页面，无法注入内容脚本是正常情况。

涉及 UI 或交互改动时，至少检查：

- 工具栏位于页面底部居中，且不会拦截工具栏外的页面点击。
- 浏览、调整、排版、输入四种模式可以正确切换。
- 选区框、数字徽标、排版拖拽点、插入按钮在滚动时仍然对齐目标元素。
- 输入面板可以正确保存或放弃文本要求。
- 调整面板可以编辑布局、宽高、间距、内边距、透明度、圆角、填充和阴影。
- 选择清单可以复制单条、复制全部、删除条目、清空全部。
- 撤销和重做在选择、排版、调整后状态正确。
- 复制出来的内容仍保持既有标题和结构化格式。

## 代码规范

- 使用原生 JavaScript 和浏览器 API。除非用户明确要求，不要引入构建工具、前端框架、TypeScript 改造或第三方依赖。
- 共享可变状态放在 `content/00-bootstrap.js` 的 `state` 对象里。
- 新增逻辑前先搜索已有 helper。不要重复实现 DOM 选择、目标识别、样式提取、历史记录、剪贴板或定位逻辑。
- 扩展 UI 的 CSS class 保持 `chat-context-picker-` 前缀。
- 扩展自己创建的 DOM，如果需要被选择逻辑忽略，应加上 `data-chat-context-picker-ui="true"`。
- 使用现有最高层级覆盖策略让扩展 UI 高于页面内容。不要随意发明新的 z-index 层级。
- 保留控件上的可访问性信息，例如 `aria-label`、`title`、`role`、`aria-pressed`、`aria-checked`。
- 工具栏和高密度操作区优先使用图标按钮。当前图标是注册在 `ICONS` 中的 Lucide 风格内联 SVG。
- 注释保持短而有用，只解释不明显的行为。

## 设计规范

视觉语言以 [design.md](./design.md) 为准。简要约束如下：

- 工具栏是深色、玻璃感的命令面板。
- 检查器和弹出面板是紧凑的白色面板，使用浅灰边框和克制阴影。
- 蓝色用于选区、焦点、激活态和可编辑状态。
- 粉红和红色只用于排版拖拽、放置目标和移动反馈。
- 控件应保持高密度、图标优先，适合频繁使用。
- 动效要短促清晰，常规控件约 `120ms`，弹出面板约 `140ms`。

## 行为边界

- Design Cursor 是本地隐私优先工具。没有明确产品要求时，不要新增网络请求、分析、远程日志或后端依赖。
- 剪贴板输出是产品界面的一部分。`# 页面修改上下文`、`### 布局调整说明`、`### 调整模式说明`、`## 目标`、`### 修改要求` 等标题应视为稳定格式。
- 输入模式支持元素目标和文本目标。排版模式和调整模式只处理元素目标。
- 层级快捷键是核心工作流：`Shift+Enter` 选择可选父级，`Enter` 返回可选子级。
- 历史记录有意限制长度。当前最多保留 50 条历史。
- 默认模式是 `调整模式`。不要轻易修改默认模式，它会影响首次打开扩展时的工作流。

## 文档规范

- 面向用户的文档保持中文，除非用户明确要求其他语言。
- README 和使用说明书的文案应直接、工作流导向，避免营销化描述。
- 如果功能改变了用户可见工作流，需要同步更新 `README.md` 或 `使用说明书.md`。
- 如果视觉规则发生变化，需要在同一次改动中同步更新 `design.md`。
