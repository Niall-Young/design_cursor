# Design System: Design Cursor

本文档记录 Design Cursor 当前实现中的视觉系统和交互规范，依据主要来自 `content.css`、`content/10-ui.js` 以及内容脚本中的运行逻辑。

## 1. Visual Theme & Atmosphere

Design Cursor 应该像一个精确的页面设计工具，而不是普通网页浮层或营销组件。它运行在任意网页之上，所以界面必须紧凑、高对比、自成体系。当前最核心的视觉组合是：底部深色悬浮命令栏、白色检查器面板，以及高亮的蓝色选区覆盖层。

界面密度偏高。大多数命令使用图标按钮，文字通过 `aria-label` 和 `title` 辅助表达，面板使用小字号和紧凑间距。整体应传达“检查、选择、直接操作”的感觉：蓝色表示目标、焦点和可编辑状态，粉红和红色表示排版移动，白色面板承载可编辑控件。

### Key Characteristics

- 底部居中的深色玻璃感工具栏。
- 白色检查器面板，浅灰边框，克制阴影。
- 明亮蓝色选区系统，用于悬停、焦点、徽标和激活态。
- 粉红和红色排版反馈，用于拖拽点、拖拽中状态和放置目标。
- 面向高频使用的紧凑专业控件。

## 2. Color Palette & Roles

| 角色 | 语义名称 | 值 | 用途 |
| --- | --- | --- | --- |
| 主选区 | Cursor Blue | `#2A59FF` | 选中描边、编辑徽标、激活对齐点、插入按钮、焦点强调。 |
| 主悬停 | Cursor Blue Hover | `#1847F2` | 蓝色徽标和插入按钮的悬停态。 |
| 开关激活 | Switch Blue | `#316BFF` | 关联模式开关的激活态。 |
| 排版强调 | Layout Pink | `#FF315F` | 排版拖拽圆点和拖拽激活态。 |
| 拖拽警示 | Drag Red | `#FF4D4F` | 拖拽中覆盖层反馈。 |
| 放置目标 | Drop Pink | `#FF4DB8` | 排版放置目标覆盖层。 |
| 工具栏表面 | Near Black | `#171717` / `rgba(23, 23, 23, 0.98)` | 主工具栏背景和主操作按钮。 |
| 面板表面 | White | `#FFFFFF` | 输入、清单、调整、颜色、阴影和菜单面板。 |
| 弱表面 | Control Gray | `#F7F7F7` | 次级按钮、分段控件底、快捷短语、计数胶囊。 |
| 边框 | Hairline Gray | `#EBEBEB` | 面板边框、输入框边框、分隔线。 |
| 主文本 | Ink | `#333333` | 面板标题、标签、输入文本。 |
| 次文本 | Muted Ink | `#666666` | 次级按钮文本、辅助标签、非激活控件。 |
| 三级文本 | Soft Muted | `#999999` | 关闭图标、元信息、弱操作。 |
| 空状态文本 | Quiet Gray | `#B4B4B4` | 空状态和弱分隔标签。 |
| Toast 表面 | Slate Toast | `rgba(15, 23, 42, 0.94)` | 顶部状态提示。 |

### Primary

- 蓝色负责“选中、焦点、激活、可编辑”。只要某个元素处于被选择或可直接操作的状态，优先使用 `#2A59FF` 或低透明度蓝色。
- 黑色负责“命令确认”。选择清单中的主操作按钮使用近黑色，而不是蓝色，这样蓝色可以继续专注表达页面目标。

### Interactive

- 焦点环通常使用 `rgba(42, 89, 255, 0.12)`，边框可提升到 `rgba(42, 89, 255, 0.36)`。
- 行选中态使用低透明度蓝色，例如 `rgba(42, 89, 255, 0.08)` 或 `rgba(42, 89, 255, 0.12)`。
- 拖拽、放置、移动反馈不要使用蓝色，应使用现有粉红和红色体系。

### Neutral Scale

- `#FFFFFF` 用于面板和可编辑区域。
- `#F7F7F7` 用于次级表面和分段控件底。
- `#EBEBEB` 用于边框和分隔线。
- `#333333`、`#666666`、`#999999`、`#B4B4B4` 构成文本层级。

### Theme Modes

当前没有浅色/深色主题切换。Design Cursor 使用固定混合主题：

- 工具栏始终是深色玻璃感，与宿主页面主题无关。
- 弹出面板和检查器始终是浅色白底、灰边框。
- 覆盖层使用亮蓝和粉红/红色，保证在大多数宿主页面上可见。

除非产品需求明确要求，不要根据宿主页面主题自动换色。

### Shadows & Depth

- 工具栏：`0 18px 48px rgba(0, 0, 0, 0.36)`，并使用 `backdrop-filter: blur(18px)`。
- 弹出面板和菜单：`0 4px 32px rgba(0, 0, 0, 0.12)`。
- Toast：`0 12px 32px rgba(15, 23, 42, 0.28)`。
- 小型激活分段控件：`0 1px 2px rgba(0, 0, 0, 0.08)`。
- 选区覆盖层优先使用描边，不使用厚重阴影，以保持几何精度。

## 3. Typography Rules

### Font Family

- 主字体：`-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`。
- 等宽字体：仅在文档或复制内容中的代码片段使用浏览器默认等宽字体。
- 字距：保持默认，不使用负字距。

### Hierarchy

| 角色 | 字体 | 字号 | 字重 | 行高 | 字距 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| 面板标题 | System UI | `14px` | `600` | `20px` | `0` | 用于 `调整清单`、`添加修改`、`调整样式`、`颜色`、`阴影`。 |
| 控件文本 | System UI | `12px` | `400-500` | `18px` | `0` | 输入、快捷短语、紧凑区块标题、菜单项、操作按钮。 |
| 清单项标题 | System UI | `12px` | `500` | `18px` | `0` | 已选目标列表标题。 |
| 元信息 | System UI | `11px` | `400` | `16px` | `0` | 计数胶囊和目标元信息。 |
| 徽标数字 | System UI | `12px` | `700` | `1` | `0` | 蓝色选中目标数字徽标。 |
| Toast | System UI | `12px` | `400` | 浏览器默认 | `0` | 简短状态反馈。 |

### Principles

- 界面文字要短，偏操作导向，例如 `复制全部`、`清空选择`、`调整样式`、`关联模式`。
- 高频操作使用图标按钮，并配套 tooltip 与可访问标签。
- 扩展 UI 内不要加入大标题、营销语或解释性长文案。

## 4. Component Stylings

### Toolbar

- 位置：固定在页面底部居中，`bottom: 32px`、`left: 50%`、`transform: translateX(-50%)`。
- 宽度：`min(360px, calc(100vw - 24px))`，移动端使用 `calc(100vw - 20px)`。
- 外壳：`inline-flex`、`gap: 16px`、`padding: 4px`、`border-radius: 10px`、深色半透明背景、1px 半透明白色边框。
- 按钮：`28px` 正方形，`6px` 内边距，`6px` 圆角，`16px` 图标。
- 悬停：白色半透明底 `rgba(255, 255, 255, 0.08)`。
- 激活：白色半透明底 `rgba(255, 255, 255, 0.12)`。
- 按下：`transform: scale(0.96)`。
- 禁用：透明度 `0.36`，不显示悬停底。
- 分隔线：`1px` 宽、`16px` 高，半透明白色。

### Selection Overlays

- 覆盖层是固定全屏层，默认不接收指针事件。
- 悬停和选中矩形使用 2px 描边。
- 选中态使用 `#2A59FF`；排版选中态也使用蓝色描边，背景保持透明。
- 编辑徽标是蓝色胶囊，最小 `28px`，白色文字，`12px` 加粗数字，阴影为 `0 4px 12px rgba(42, 89, 255, 0.24)`。
- 角点控制柄为 `10px` 白底方块，2px 蓝色描边，放在选框外侧。

### Layout Controls

- 拖拽点是 `12px` 圆形描边，颜色 `#FF315F`。
- 悬停时圆点填充为 `#FF315F`。
- 拖拽激活时增加柔和粉色外圈。
- 插入按钮使用 `#2A59FF`、`6px` 圆角、`12px` 图标，悬停时阴影增强。
- 插入占位指示是沿移动方向出现的 4px 蓝色胶囊。
- 放置目标使用 `#FF4DB8` 描边和非常浅的粉色填充。

### Popovers And Menus

- 输入面板：`380px` 宽，`16px` 圆角，白底，`#EBEBEB` 边框，`0 4px 32px rgba(0, 0, 0, 0.12)` 阴影。
- 调整面板：`min(320px, calc(100vw - 20px))`，最大高度 `min(720px, calc(100vh - 32px))`，内容区可滚动。
- SVG 资源目标的调整面板隐藏布局区，底部使用与「清空选择」一致的浅灰通栏按钮下载资源。
- 颜色和阴影面板：宽度约 `280-294px`，延续同一白色面板样式。
- 宽高和间距菜单：紧凑固定菜单，`12px` 圆角，`6px` 内边距。
- 打开/关闭动效：从 `translateY(8px) scale(0.98)`、透明，过渡到 `translateY(0) scale(1)`、可见，时长 `140ms ease`。

### Lists And Actions

- 选择清单最大高度 `320px`，使用细滚动条。
- 清单项使用 `10-12px` 内边距、`10px` 圆角、白底、`#EBEBEB` 边框。
- 空状态居中，图标优先，文字使用 `#B4B4B4`。
- 主操作按钮：近黑色背景、白色文字、`8px` 圆角，最小高度 `36px`。
- 次操作按钮：`#F7F7F7` 背景、弱文本、`8px` 圆角。

### Inputs And Interactive Controls

- 输入行经过紧凑化后最小高度为 `26px`。
- 边框为 `#EBEBEB`，常用圆角为 `6px`。
- 焦点态使用蓝色边框和低透明度蓝色外圈。
- 分段控件放在 `#F7F7F7` 底上，间隔 `2px`，激活段为白底。
- 开关尺寸为 `34px` by `20px`；激活态为 `#316BFF`，白色 `14px` thumb。
- 对齐控制是 3 by 3 网格，默认灰色小点，激活后显示蓝色轴向标记。

### Color And Shadow Controls

- 色块为 `14px` 或 `16px` 方形，`3px` 圆角。
- 透明度使用棋盘格表达。
- 饱和度/明度面板高度 `256px`，`12px` 圆角，使用 crosshair 光标。
- 滑杆高度 `20px`，胶囊轨道，`12px` 圆形 thumb。
- 默认新增填充为黑色 20% 透明度。
- 默认阴影参数为 `x=0`、`y=4`、`blur=4`、透明度 20%。

### Toasts

- Toast 固定在顶部居中，`top: 24px`。
- 使用深色 slate 胶囊，白字，`10px 14px` 内边距，`999px` 圆角。
- 文案只做短状态反馈，例如 `已复制元素上下文`、`已打开调整面板`、`复制失败`。

## 5. Layout Principles

### Spacing System

- 核心间距值为 `2`、`4`、`6`、`8`、`10`、`12`、`16px`。
- 高密度检查器使用 `8px` 网格间距和 `10-12px` 面板内边距。
- 工具栏使用更大的 `16px` 间隔，因为它是纯图标命令组，需要清晰分组。

### Grid & Container

- 工具栏固定在底部居中，并受最大宽度约束。
- 弹出面板采用 fixed 定位，锚定目标元素或工具栏控件。
- 检查器控件偏好两列网格，用于数字输入和紧凑选项组。
- 快捷短语在桌面端使用两列网格，在 `520px` 以下折叠为一列。

### Whitespace Philosophy

- 留白要实用，不追求宽松展示。
- 控件在小尺寸下仍应可扫读。
- 扩展 UI 内避免装饰卡片、hero 区、大字号排版和解释性长文。

### Border Radius Scale

- Micro：`3px`，用于色块。
- Control：`6px`，用于图标按钮和输入行。
- Standard：`8px`，用于快捷短语、次级表面、操作按钮、激活菜单行。
- Toolbar/list item：`10px`。
- Menus：`12px`。
- Popovers：`16px`。
- Pills/circles：`999px`。

## 6. Depth & Elevation

| 层级 | 样式 | 用途 |
| --- | --- | --- |
| Flat | 透明或白底，无阴影 | 图标按钮、行内控件。 |
| Ring | 1px 边框加轻微蓝色焦点环 | 聚焦输入行、激活宽高/间距控件。 |
| Panel | 白底、灰边框、`0 4px 32px` 阴影 | 弹出面板和菜单。 |
| Command | 深色半透明、模糊、深阴影 | 悬浮工具栏。 |
| Page marker | 2px 亮色描边，无面板阴影 | 悬停和选中目标覆盖层。 |

### Depth Principles

- 景深用于把扩展 UI 从宿主页面中分离出来。
- 选区覆盖层必须保持几何精确，不要添加厚重阴影。
- 只有工具栏使用 blur，不要把检查器面板做成玻璃拟态。
- 嵌套层级要浅。菜单可以浮在面板上，但不要制造层层卡片。

## 7. Interaction Patterns

- 模式为 `浏览模式`、`调整模式`、`排版模式`、`输入模式`。
- 默认模式是 `调整模式`。
- 工具栏模式按钮需要激活态，并配合 toast 确认。
- `Shift+Enter` 选择父级元素；`Enter` 选择记忆过或最近的子级元素。
- `Esc` 先关闭当前面板，再关闭整个扩展。
- `Command/Ctrl+C` 复制当前选中上下文。
- `Command/Ctrl+Z` 和 `Shift+Command/Ctrl+Z` 用于撤销和重做。
- `Alt` 加数字输入横向拖拽用于连续调整数值。
- 滚动和窗口尺寸变化时，弹出面板需要重新定位。

## 8. Do's and Don'ts

### Do

- 新 UI 保持 `chat-context-picker-` class 前缀。
- 蓝色只用于选区、焦点、激活可编辑态和插入操作。
- 粉红和红色只用于排版移动和放置反馈。
- 按钮文案保持短而直接。
- 工具栏保持图标优先和高密度。
- 控件继续维护键盘和屏幕阅读器可用性。

### Don't

- 不要在注入 UI 中添加营销区、引导卡片或装饰背景。
- 不要使用宿主页面的字体或颜色 token 来渲染扩展控件。
- 不要给选区矩形添加大面积阴影。
- 不要把工具栏图标控件替换成大文本按钮。
- 不要为常规状态引入新的主色系，当前蓝色、灰阶、粉红/红色已经覆盖主要语义。
- 不要让 UI 依赖后端、远程资源或分析服务。

## 9. Responsive Behavior

### Breakpoints

| 名称 | 宽度 | 关键变化 |
| --- | --- | --- |
| Mobile | `max-width: 520px` | 工具栏宽度收紧到 `calc(100vw - 20px)`，快捷短语变一列，清单操作按钮堆叠。 |
| Compact viewport | `calc(100vw - 20px/32px)` 约束 | 输入、调整、颜色、阴影、宽高、间距面板都按视口宽度钳制。 |
| Desktop | 大于 `520px` | 工具栏保持紧凑，快捷短语和清单操作使用两列。 |

### Touch Targets

- 主工具栏按钮为 `28px` 正方形，这是面向桌面扩展的紧凑尺寸。
- 弹出面板主操作按钮紧凑化后至少 `36px` 高。
- 检查器内的小图标按钮为 `22-24px`，只用于高密度专家控件。

### Collapsing Strategy

- 除非明确要求，不要把面板改成全屏移动端抽屉。
- 保持 fixed popover 模型，并通过视口宽度限制避免溢出。
- 空间不足时堆叠操作按钮和快捷短语。
- 保持检查器主体可滚动，并继续使用最大高度约束。

## 10. Content & Messaging Patterns

- 语气直接，中文优先，围绕具体工作流。
- 优先使用描述即时操作的动词，例如 `复制`、`清空`、`关闭`、`添加`、`移除`、`选择`。
- Toast 只用一句短句确认结果或解释阻塞。
- 剪贴板 payload 是结构化指令，应该保持事实性，不要写成聊天语气。

## 11. Agent Prompt Guide

### Quick Color Reference

- 主选区：`#2A59FF`。
- 选区悬停：`#1847F2`。
- 排版拖拽：`#FF315F`。
- 放置目标：`#FF4DB8`。
- 工具栏：`rgba(23, 23, 23, 0.98)`。
- 面板表面：`#FFFFFF`。
- 边框：`#EBEBEB`。
- 文本：`#333333`。

### Example Component Prompts

- 工具栏：构建一个底部居中的深色玻璃感工具栏，使用 28px 图标按钮、16px 图标、10px 圆角、浅白边框、深阴影和白色半透明激活态。
- 选区覆盖层：使用精确的 2px 蓝色目标描边、白色角点控制柄和蓝色数字胶囊徽标，覆盖层必须几何精确，不要厚重阴影。
- 调整检查器：构建一个紧凑的 320px 白色检查器，14px 区块标题、12px 控件文本、26px 输入行、两列数字网格、分段控件和蓝色焦点环。
- 排版反馈：使用粉红圆形拖拽点和蓝色胶囊插入标记，粉红/红色只表达移动和放置状态。

### Quick Summary

Design Cursor 是叠加在任意网页上的紧凑专家工具。它的识别度来自深色悬浮命令栏、精确蓝色选区覆盖层、白色检查器面板和高密度图标控件。界面要克制、高对比、功能优先。蓝色表示选中或可编辑，粉红和红色表示排版移动。动效短促清晰。复制、模式切换和样式调整都是产品界面，不是附带实现。

## 12. Evidence Notes

观察依据：

- `content.css`：颜色、间距、尺寸、动效、阴影和响应式行为。
- `content/10-ui.js`：工具栏、清单、输入面板、调整面板结构。
- `content/00-bootstrap.js`：模式文案、图标注册、默认状态。
- `content/30-selection-layout.js`、`content/40-adjust.js`、`content/50-runtime.js`：交互行为。

本仓库是直接从源码加载的浏览器扩展，不是独立网页，因此本文档没有额外生成网页截图证据。
